//! Command-line interface parsing for the `dry4rust` binary.
//! Confines the `clap` dependency to this module.
//!
//! Scaffold stub — the `clap`-derived CLI (flags such as `--threshold`,
//! `--min-lines`, `--min-nodes`, `--format`/`--json`/`--text`) is wired here
//! in a later task (T7). No argument parsing exists yet.
