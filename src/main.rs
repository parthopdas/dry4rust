//! `dry4rust` binary entry point: `anyhow` wiring and exit codes.
//!
//! Scaffold stub — a no-op that exits 0. Argument parsing and detection are
//! wired in later tasks.

mod cli;

use anyhow::Result;

fn main() -> Result<()> {
    Ok(())
}
